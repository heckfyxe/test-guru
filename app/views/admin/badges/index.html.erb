<h1><%= t(:badges) %></h1>

<%= link_to t(:create_new), new_admin_badge_path %>

<table>
  <tr>
    <th><%= t(:title) %></th>
    <th colspan="2"><%= t(:actions) %></th>
  </tr>
  <%= render @badges %>
</table>

<%= link_to t(:back), admin_tests_path %>