<tr>
  <td><%= answer.text %></td>
  <td><%= answer.correct ? "+" : "-" %></td>
  <td><%= link_to t(:edit), edit_admin_answer_path(answer) %></td>
  <td><%= link_to t(:delete), admin_answer_path(answer), method: :delete, data: { confirm: t(:are_you_sure) } %></td>
</tr>
